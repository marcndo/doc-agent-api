# For parsing web pages
beautifulsoup4
# For the web server API
fastapi
# For running the FastAPI server
uvicorn[standard]
# For handling PDFs
pypdf
# For creating text embeddings
sentence-transformers
# For the vector database
faiss-cpu
# To interact with LLMs easily
langchain
# For LLM API integration (we'll start with OpenAI for simplicity)
langchain-openai
# For reading environment variables (like API keys)
python-dotenv